# calculate the absolute value of the difference between each pair of items
# 
# set max to 0
# for i in arr
#    max = abs(arr[i] - arr[i-1]) if 

def arrayMaximalAdjacentDifference(inputArray):
    max = 0
    for i in range(1,len(inputArray))
        diff = abs(arr[i] - arr[i-1])
        [max = diff if diff > max]
        print(max)